import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';
export default function Register(){const{register,loading}=useAuth(),nav=useNavigate(),[f,setF]=useState({name:'',email:'',phone:'',password:'',confirm:''}),[err,setErr]=useState('');const u=e=>setF({...f,[e.target.name]:e.target.value});const go=async e=>{e.preventDefault();if(f.password.length<6)return setErr('Password must be at least 6 characters.');if(f.password!==f.confirm)return setErr('Passwords do not match.');try{await register(f.name,f.email,f.password,f.phone);nav('/packages')}catch(x){setErr(x.message)}};return <section className="auth-page"><form className="form-card auth-card" onSubmit={go}><span className="eyebrow">JOIN TRAVELAURA</span><h1>Create account</h1><label>Name<input required name="name" value={f.name} onChange={u}/></label><label>Email<input required type="email" name="email" value={f.email} onChange={u}/></label><label>Phone<input required name="phone" value={f.phone} onChange={u}/></label><label>Password<input required type="password" name="password" value={f.password} onChange={u}/></label><label>Confirm password<input required type="password" name="confirm" value={f.confirm} onChange={u}/></label>{err&&<p className="form-error">{err}</p>}<button className="button full" disabled={loading}>Create account</button><p>Already registered? <Link className="text-link" to="/login">Sign in</Link></p></form></section>}
