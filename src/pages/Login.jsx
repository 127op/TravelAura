import{useState}from'react';import{Link,useLocation,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{isDemo}from'../lib/firebase';
export default function Login(){const{login,google,loading}=useAuth(),nav=useNavigate(),loc=useLocation(),[f,setF]=useState({email:'',password:''}),[err,setErr]=useState('');const go=async e=>{e.preventDefault();setErr('');try{const u=await login(f.email,f.password);nav(u.role==='admin'?'/admin':(loc.state?.from||'/my-bookings'))}catch(x){setErr(x.message)}};const g=async()=>{try{await google();nav('/my-bookings')}catch(x){setErr(x.message)}};return <section className="auth-page"><form className="form-card auth-card" onSubmit={go}><span className="eyebrow">WELCOME BACK</span><h1>Sign in</h1>{isDemo&&<div className="demo-note"><b>Demo mode:</b> use <code>user@travelaura.com</code> or <code>admin@travelaura.com</code>. Any password works.</div>}<label>Email<input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input required type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label>{err&&<p className="form-error">{err}</p>}<button className="button full" disabled={loading}>Sign in</button><button className="button secondary full" type="button" onClick={g}>Continue with Google</button><p>New here? <Link className="text-link" to="/register">Create an account</Link></p></form></section>}
