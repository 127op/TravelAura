import {reportError} from '../lib/feedback';
import {useEffect,useState} from 'react';import{Link}from'react-router-dom';import{ArrowRight,Compass,ShieldCheck,HeartHandshake}from'lucide-react';import SearchBar from'../components/SearchBar';import PackageCard from'../components/PackageCard';import DestinationCard from'../components/DestinationCard';import{db}from'../lib/dataService';import{isDemo}from'../lib/firebase';import{testimonials}from'../data/demo';
export default function Home(){const[p,setP]=useState([]),[d,setD]=useState([]),[stories,setStories]=useState(isDemo?testimonials:[]);useEffect(()=>{Promise.resolve(db.getPackages()).then(setP).catch(reportError);Promise.resolve(db.getDestinations()).then(setD).catch(reportError);if(!isDemo)Promise.resolve(db.getReviews()).then(rows=>setStories(rows.map(r=>({...r,name:r.userName,text:r.comment})))).catch(reportError)},[]);return <><section className="hero"><div className="hero-copy"><span className="eyebrow">TRAVEL, REIMAGINED</span><h1>Discover your<br/><em>next adventure.</em></h1><p>Find the right place, choose your dates and travelers, book a curated package and manage everything in one place.</p></div><SearchBar/><div className="hero-note">✦ &nbsp;Curated journeys across India and beyond</div></section><section className="section"><div className="section-head"><div><span className="eyebrow">POPULAR DESTINATIONS</span><h2>Where do you want to go?</h2></div><Link to="/destinations" className="text-link">View all <ArrowRight size={17}/></Link></div><div className="destination-grid">{d.slice(0,4).map(x=><DestinationCard key={x.id} item={x}/>)}</div></section><section className="section compact-section"><div className="section-head"><div><span className="eyebrow">FEATURED PACKAGES</span><h2>Journeys worth taking</h2></div><Link to="/packages" className="text-link">View packages <ArrowRight size={17}/></Link></div><div className="trip-grid">{p.slice(0,3).map(x=><PackageCard item={x} key={x.id}/>)}</div></section><section className="values"><div><Compass/><h3>Curated experiences</h3><p>Clear itineraries, hotels, meals, transport and inclusions before you book.</p></div><div><HeartHandshake/><h3>Simple manual payment</h3><p>Scan the UPI QR, confirm your payment and track admin verification in your account.</p></div><div><ShieldCheck/><h3>Easy management</h3><p>Bookings, cancellations, reviews and status updates stay in one dashboard.</p></div></section><section className="section"><div className="section-head"><div><span className="eyebrow">TRAVELER STORIES</span><h2>What travelers say</h2></div></div><div className="testimonial-grid">{stories.slice(0,4).map(t=><article className="testimonial" key={t.id}><div>{'★'.repeat(t.rating)}</div><p>“{t.text}”</p><strong>{t.name}</strong></article>)}</div></section><section className="inspiration"><div><span className="eyebrow">PLAN WITH CONFIDENCE</span><h2>From inspiration<br/><em>to confirmation.</em></h2><p>Search, compare, book and track your trip without leaving TravelAura.</p><Link to="/packages" className="button secondary">Explore packages <ArrowRight size={17}/></Link></div><div className="inspire-image"><img src="https://images.unsplash.com/photo-1469474968028-56623f02e42e?auto=format&fit=crop&w=1000&q=80" alt="mountain travel"/></div></section></>}
