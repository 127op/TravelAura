import{useState}from'react';import{Plus}from'lucide-react';
const questions=[['What is included in a TravelAura package?','Each package page clearly lists hotel, meals, transport, activities, inclusions and exclusions. Always review these details before booking.'],['How do payments work?','TravelAura uses a manual UPI QR workflow for this assessment. Scan the QR in your UPI app, pay the exact amount, then submit your payment confirmation. The transaction/UTR ID and screenshot are optional. An admin verifies the payment and updates the booking.'],['Can I change or cancel my booking?','You can request cancellation for eligible upcoming bookings from My Bookings. An admin reviews the request. Because there is no payment gateway, any refund is handled manually according to the package policy.'],['How does demo mode work?','If Firebase environment variables are not configured, the app uses localStorage so the complete assessment can be demonstrated locally. Demo data is not production storage.'],['Can I leave a review?','After a booking is marked completed, you can submit a rating and review from the booking details page. It appears publicly after admin approval.'],['Do coupons expire?','Yes. Coupons can have a minimum booking amount, expiry date, usage limit and active/inactive status.']];
export default function FAQ(){const[open,setOpen]=useState(0);return <section className="faq section"><div><span className="eyebrow">GOOD TO KNOW</span><h1>Frequently asked<br/><em>questions.</em></h1><p className="muted">A few answers before you set off.</p></div><div className="faq-list">{questions.map(([q,a],i)=><div className={open===i?'faq-item open':'faq-item'} key={q}><button onClick={()=>setOpen(open===i?-1:i)}><span>{q}</span><Plus size={19}/></button>{open===i&&<p>{a}</p>}</div>)}</div></section>}
