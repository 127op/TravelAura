import {Link} from 'react-router-dom';
export default function DestinationCard({item}){return <article className="destination-card">{item.image&&<img src={item.image} alt={item.name}/>}<div><span className="eyebrow">{item.category}</span><h3>{item.name}</h3><p>{item.state||item.country}, {item.country}</p><div className="destination-facts"><span>Best: {item.bestTime}</span><strong>From ₹{Number(item.budget).toLocaleString('en-IN')}</strong></div><Link className="text-link" to={`/destinations/${item.id}`}>View details →</Link></div></article>}
