import {Link,useLocation} from 'react-router-dom';import{Clock,Star}from'lucide-react';
export default function PackageCard({item}){const{search}=useLocation();const img=item.images?.[0]||item.image;return <article className="trip-card"><Link to={`/packages/${item.id}${search}`} className="trip-image-wrap">{img&&<img className="trip-image" src={img} alt={item.name}/>}<span className="trip-tag">{item.category}</span></Link><div className="trip-copy"><div className="trip-meta"><span>{item.destination}</span><span><Star size={14} fill="currentColor"/> {item.rating}</span></div><h3><Link to={`/packages/${item.id}${search}`}>{item.name}</Link></h3><p>{item.hotel} · {item.meals}</p><div className="trip-bottom"><span><Clock size={15}/> {item.duration} days</span><strong>₹{Number(item.pricePerAdult||0).toLocaleString('en-IN')} <small>/adult</small></strong></div></div></article>}
